info:
  name: get assignment Student
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/me/class/37/assignmentsStudent
  auth:
    type: bearer
    token: "{{auth_secret_02et}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
