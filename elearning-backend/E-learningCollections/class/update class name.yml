info:
  name: update class name
  type: http
  seq: 11

http:
  method: PUT
  url: http://localhost:5000/api/admin/classes/2
  body:
    type: json
    data: |-
      {
        "class_name" : "XII DKV"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
