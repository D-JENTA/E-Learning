info:
  name: getUserClassDetails
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/users/:id_user/class-details
  params:
    - name: id_user
      value: ""
      type: path
  body:
    type: text
    data: "{\r\n\r\n}"
  auth:
    type: bearer
    token: "{{supabase_service_role_api_key_1cf6}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
