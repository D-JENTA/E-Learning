info:
  name: user delete
  type: http
  seq: 1

http:
  method: DELETE
  url: http://localhost:5000/api/admin/users/:id_user
  params:
    - name: id_user
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
